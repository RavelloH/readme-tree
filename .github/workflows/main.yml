on: [push]

jobs:
  tree:
    runs-on: ubuntu-latest
    name: readme-tree
    steps:
      - uses: actions/checkout@v3
      - id: foo
        uses: RacelloH/readme-tree@v0.1
        with:
          showall: 'yes'
